// Twitter / X utilise le même format que Open Graph (1200x630 summary_large_image)
// On réutilise exactement le même rendu pour cohérence visuelle.
export { default, alt, size, contentType } from "./opengraph-image";
